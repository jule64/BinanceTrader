
# TODOs

- [x] create a read-only version of the app
- [x] add persistence of tickers added via the UI (currently the list resets to the one defined in appData.json)
- [x] add persistence of trade up/down alerts so they don't disappear after restart
- [x] add 24h price change column
- [ ] add user login
- [ ] add charting
- [ ] stop-loss / take-profit orders — set automatic exit levels per position
- [ ] order history log — display recent fills in a panel
- [ ] PnL per position — show unrealized gain/loss per coin, not just total
- [ ] multiple alerts per ticker — currently only one up/one down per coin
- [ ] alert history — log when alerts were triggered
- [ ] persistent ticker removal — currently tickers can be added but not removed via the UI
- [ ] color-coded price cells — flash green/red on price movement
- [ ] sortable table columns — sort by 24h%, volume, etc.
- [ ] volume spike detection — highlight tickers with unusual volume
- [ ] confirmation dialog before placing orders
- [x] max order size guard — warn if order exceeds X% of balance
- [ ] mobile-friendly layout

